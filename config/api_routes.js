const host = 'https://app-stocker.herokuapp.com'

/* INVOICES */
export const invoicesUrl      = `${host}/api/v1/invoices`
export const createInvoiceUrl = `${host}/api/v1/invoices`

/* PRODUCTS */
export const productsUrl      = `${host}/api/v1/products`
export const createProductUrl = `${host}/api/v1/products`

